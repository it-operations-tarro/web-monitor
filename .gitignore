node_modules/
.env
*.log

# Runtime files — never commit these
collector/logs.db
collector/logs.db-wal
collector/logs.db-shm
collector/config.json

# One-time SQLite→MySQL migration dump — contains real employee data, already
# imported into the live DB. Kept local only; do not commit.
collector/schema/data_export.sql

# Monthly log archives (gzipped SQL) — real activity data, generated at runtime.
collector/archives/

# AWS CLI installer artifacts
aws/
awscliv2.zip

# Chrome extension signing key — NEVER commit (fixes the extension ID;
# whoever holds it can sign a build our fleet force-installs)
extension.pem
*.pem
